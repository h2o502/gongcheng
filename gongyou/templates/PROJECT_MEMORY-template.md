# 项目框架记忆

> 这是 AI 每次会话必读的文件。控制在 200 行内，只放架构、命令、硬约束。

## 项目概述

<!-- 一句话描述项目是什么 -->
<项目名称>：<简要描述>

## 架构

<!-- 几句话或一个简单的结构图，让 AI 快速理解项目布局 -->
```
<项目根>/
├── src/
│   ├── <模块1>/    # <职责>
│   ├── <模块2>/    # <职责>
│   └── <模块3>/    # <职责>
├── tests/
└── <配置目录>/
```

## 常用命令

```bash
# 构建
<build 命令>

# 测试
<test 命令>

# 部署
<deploy 命令>

# 开发
<dev 命令>
```

## 硬约束

<!-- 不可违反的规则 -->
- <约束1>
- <约束2>

## 高风险文件（改动前必须用户确认）

<!-- 列出改了容易出问题的文件/目录，用 glob pattern -->
- src/auth/**          # 鉴权链路
- src/db/migrations/** # 数据库迁移
- src/middleware/**    # 中间件
- **/config.{ts,json}  # 配置文件
- **/*.sql             # SQL 脚本

## 环境信息

- **语言/框架**：<如 TypeScript / React / Go>
- **数据库**：<如 MySQL 8.0 / PostgreSQL 15>
- **运行时**：<如 Node 22 / Go 1.22>
- **端口**：<如 5232>

## 传火规则

- 改任何文件前，先运行 `check-impact.sh <文件>` 检查影响
- 高风险文件改动必须等用户确认
- 踩坑后在代码旁补 `#@` 标注
- 会话结束前更新 `memory.md`
